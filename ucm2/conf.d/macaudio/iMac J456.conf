Syntax 6

SectionUseCase."HiFi" {
	# Well, sure, the iMac isn't a laptop, but it needs essentially
	# the same config. (That is until there will be support for the
	# internal mic on the laptops.)
	Comment "Default"
	File "/conf.d/macaudio/laptops/4spk/HiFi.conf"
}
